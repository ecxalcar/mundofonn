<div class="wrapper-details bg-light border p-4 rounded-3">
    <h4>Account Details</h4>
    <p>{{ customer.default_address | format_address }}</p>
    <a href="{{ routes.account_addresses_url }}">
        {{ 'View all addresses' }} ({{ customer.addresses_count }})
    </a>
</div>